spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file:///C:/projects/microservice_architecture/config-server-repository
          clone-on-start: true
    fail-fast: true