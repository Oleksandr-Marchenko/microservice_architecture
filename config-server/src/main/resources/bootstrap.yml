spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Oleksandr-Marchenko/spring-cloud-config-server-repository
          clone-on-start: true
          username: Oleksandr-Marchenko
          password: *******
      fail-fast: true
  security:
    user:
      name: spring_cloud_user
      password: springCloud_Pwd!